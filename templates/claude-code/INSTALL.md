# Claude Code Template Install

Status: inactive.

No Claude Code install flow is currently implemented.
The current global setup is Codex-first:

```bash
python3 scripts/install_global.py setup --provider codex
python3 scripts/install_global.py doctor --provider codex
```
